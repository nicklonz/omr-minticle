<%= form_for(@minticle) do |f| %>
  <% if @minticle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@minticle.errors.count, "error") %> prohibited this minticle from being saved:</h2>

      <ul>
      <% @minticle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="form-group">
    <%= f.label :description %>
    <%= f.text_field :description, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
